import 'jquery-minicolors'

UI.load(() =>
  // =====================================================
  // color pickers
  // =====================================================

  $('.color-picker-text-field').minicolors()
);
